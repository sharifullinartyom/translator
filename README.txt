База на h2 
запрос на создание таблицы (CREATE TABLE INFO(ID INT PRIMARY KEY AUTO_INCREMENT, TIME VARCHAR(255) , PARAM VARCHAR(255), IP VARCHAR (255));)
 
http://localhost:8080/Translate/api//trans/"сюда писать слова для перевода"
 
в фале application.yml можно менять порт
 
запускается проект из idea
 
в DB-config можно менять доступы к бд